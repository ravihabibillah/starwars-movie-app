abstract class AppRoutes {
  AppRoutes._();
  static const home = AppPaths.home;
  static const movieDetail = AppPaths.movieDetail;
  // static const mapFull = AppPaths.mapFull;
}

abstract class AppPaths {
  AppPaths._();
  static const home = '/home';
  static const movieDetail = '/movie/detail';
  // static const mapFull = '/map/full';
}
